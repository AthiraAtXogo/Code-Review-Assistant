{
  "$schema": "https://code.claude.com/schemas/mcp.json",
  "_comment": "MCP server configuration for Code Review Assistant",

  "mcpServers": {
    "code-review-assistant": {
      "_comment": "Code analysis and explanation tools",
      "command": "node",
      "args": [".output/server/index.mjs"],
      "env": {
        "NITRO_PORT": "3001",
        "NODE_ENV": "production"
      }
    }
  }
}
